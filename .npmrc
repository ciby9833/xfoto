# Skip native build scripts during npm install.
# Our postinstall patches @electron/node-gyp for VS 2026 support, then
# runs electron-rebuild to compile native modules (better-sqlite3) correctly.
# After `npm install`, run `npm run postinstall` to complete setup.
ignore-scripts=true
